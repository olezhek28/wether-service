# Проект: Рефакторинг с чистой архитектурой

Проект был рефакторизован с применением принципов чистой архитектуры для улучшения структуры кода, тестируемости и поддерживаемости.

## 🚀 Быстрый старт

### Предварительные требования
- Установленный Docker
- Установленный Go

### Запуск проекта
Для запуска нужно:
  - Включить Docker
  - В консоли прописать ```docker compose up```
  - Запустить сервис через консоль ``` go run ../cmd/server/main.go  --config_path=./config/local.yaml```
  - Добавить Env файлик c такими полями:
    ```
    CONFIG_PATH=./config/local.yaml
    DB_HOST=localhost
    DB_PORT=54321
    DB_PASSWORD=parol
    DB_NAME=weather
    SSL_MODE=disable
    DB_USER=olezhek28
    ```
